# 统一用 LF 入库，检出时按平台还原。避免 Windows 和 Linux 之间来回改行尾产生噪音 diff
* text=auto eol=lf

# 这些必须是 LF，否则在服务器上执行会报 bad interpreter
*.sh text eol=lf

# 二进制，别做任何转换
*.png binary
*.jpg binary
*.ico binary
*.exe binary

# Windows 批处理和 PowerShell 脚本保持 CRLF：cmd.exe 遇到 LF 的 .bat 在 goto/标签处会出怪问题
*.bat text eol=crlf
*.ps1 text eol=crlf

*.jks binary
*.apk binary
*.asar binary
